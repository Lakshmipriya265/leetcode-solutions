class Solution:
    def isPalindrome(self, s: str) -> bool:
        x = ''.join(ch.lower() for ch in s if ch.isalnum())
        for i in range(len(x)//2):
            if x[i] != x[-(i+1)]:
                return False
        return True 